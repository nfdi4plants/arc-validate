module DirTokenizerTests

open Expecto


// TO-DO: implement these
[<Tests>]
let ``DirTokenizer Tests`` =
    ptestList "DirTokenizer Tests" [
        ptest "tokenizeRelDirectory" {()}
        ptest "tokenizeDirectory" {()}
        ptest "tokenizeFilesWithRelDirectory" {()}
        ptest "tokenizeFiles" {()}
    ]