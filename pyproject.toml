[project]
name = "arc-validate-portable-tests"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "fable-library==5.11.0",
]

[dependency-groups]
docs = [
    "mkdocs-material>=9.5",
]

[tool.uv]
package = false
